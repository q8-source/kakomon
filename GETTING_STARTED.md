# 🎓 初心者向け - Kakomon 使い始めガイド

Kakomon（行政書士試験過去問学習アプリ）へようこそ！このガイドでは、最初の一歩から始めます。

---

## 📋 必要なもの

1. **Node.js**（バージョン 18 以上）
   - [nodejs.org](https://nodejs.org) からダウンロードしてインストール
   - インストール後、ターミナル/コマンドプロンプトで `node -v` と入力して、バージョン番号が表示されれば OK

2. **テキストエディタ**（任意）
   - コードを編集したい場合のみ必要
   - 例: VS Code, Sublime Text など

---

## 🚀 起動手順（3 ステップ）

### ステップ 1: リポジトリを取得
```bash
git clone https://github.com/q8-source/kakomon.git
cd kakomon
```

### ステップ 2: スタートスクリプトを実行

**Windows ユーザー:**
1. エクスプローラーで kakomon フォルダを開く
2. `start.bat` をダブルクリック
3. コマンドプロンプトが立ち上がり、自動的にセットアップと起動が始まります

**Mac ユーザー:**
1. ターミナルを開く
2. kakomon フォルダに移動: `cd kakomon`
3. コマンド実行: `bash start.sh`

**Linux ユーザー:**
1. ターミナルを開く
2. kakomon フォルダに移動: `cd kakomon`
3. コマンド実行: `bash start.sh`

### ステップ 3: ブラウザで開く
スクリプトが実行されて、以下のメッセージが表示されたら、ブラウザで開いてください：

```
🌐 http://localhost:3000
```

ブラウザのアドレスバーに上記 URL を入力して Enter を押します。

---

## ✅ アプリケーションの使い方

1. **問題を選択**
   - 左側のリストから解きたい問題をクリック

2. **フィルタで絞込（オプション）**
   - 年度や科目で問題を絞り込むことができます

3. **問題に回答**
   - 選択肢をクリックして選択
   - 「解答する」ボタンをクリック

4. **結果を確認**
   - 正誤判定と解説が表示されます
   - 次の問題に進むには「次へ」をクリック

5. **成績確認**
   - すべての問題を解答したら「結果を見る」で総合スコアを確認

---

## 🐛 トラブルシューティング

### 問題: "Node.js がインストールされていません"

**解決方法:**
1. [nodejs.org](https://nodejs.org) を開く
2. 推奨版（LTS）をダウンロード
3. インストーラーを実行し、指示に従ってインストール
4. インストール後、ターミナル/コマンドプロンプトを再起動
5. `node -v` コマンドで確認

### 問題: "ポート 3000 は既に使用されています"

**解決方法:**
- 他のアプリケーション（別の Kakomon インスタンス、他の開発サーバーなど）が同じポートを使用しています
- その他のアプリケーションを終了するか、別のターミナルで開始してください
- または、強制的に終了したい場合:

  **Windows:**
  ```bash
  netstat -ano | findstr :3000
  taskkill /PID <PID番号> /F
  ```

  **Mac/Linux:**
  ```bash
  lsof -i :3000
  kill -9 <PID番号>
  ```

### 問題: "npm: コマンドが見つかりません"

**解決方法:**
- Node.js がプロパティインストールされていない可能性があります
- Node.js を再インストールしてください
- ターミナル/コマンドプロンプトを再起動してからお試しください

### 問題: "ブラウザで localhost:3000 に接続できない"

**解決方法:**
1. ターミナル/コマンドプロンプトにエラーメッセージがないか確認
2. エラーがあれば、「トラブルシューティング」セクション内で検索
3. 数秒待ってからリロード（F5 キーを押す）
4. ファイアウォールがアプリケーションをブロックしていないか確認

### 問題: 依存パッケージのインストール時にエラーが発生

**解決方法:**
```bash
# 手動でインストール
npm run install:all
```

もし上記でもエラーが続く場合：
```bash
# キャッシュをクリア
npm cache clean --force

# 再度インストール
npm run install:all
```

---

## 💡 便利な知識

### サーバーを停止するには
ターミナル/コマンドプロンプトで **Ctrl+C** を押します。

### 手動でコマンドを実行したい場合

**サーバーとクライアント両方を起動:**
```bash
npm run dev
```

**サーバーのみ起動:**
```bash
npm run dev:server
```

**クライアントのみ起動:**
```bash
npm run dev:client
```

### 問題データを確認・編集したい場合

問題データは `server/data/questions.json` に格納されています。

テキストエディタで開いて編集できます。

新しい問題の追加方法は、メインの [README.md](./README.md) を参照してください。

---

## 📚 次のステップ

- 各 README ファイルで詳細な情報を確認
- API エンドポイントについては [README.md](./README.md) の「API エンドポイント」セクションを参照
- 開発環境の構築方法については [README.md](./README.md) を参照

---

## ❓ よくある質問

**Q: データはどこに保存されていますか？**
A: 問題データは `server/data/questions.json` に保存されています。選択済みの回答などのセッションデータはブラウザに保存されます。

**Q: 問題を追加することはできますか？**
A: はい、できます。`server/data/questions.json` に新しい問題を JSON 形式で追加してください。詳細は README を参照。

**Q: インターネット接続は必要ですか？**
A: いいえ、ローカルで完全に動作します。インターネット接続は不要です。

**Q: 複数のユーザーで同時に使用できますか？**
A: はい、ローカルネットワークを通じて複数ユーザーがアクセスできます。詳細は README を参照。

---

質問や問題がある場合は、[GitHub Issues](https://github.com/q8-source/kakomon/issues) で報告してください。

楽しい学習を！🎓
